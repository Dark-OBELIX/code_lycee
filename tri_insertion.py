def tri_insertion(L):
	n = len(L)
	if ...:
		return L

	for j in range(1, n):
		e = L[j]
		i = j

		while i > 0 and L[i-1] > ...:

		if i != j:
			for k in range (j,i,...):
				L[k] = L[...]
			L[i] = ...
		return L
		